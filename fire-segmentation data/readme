annotations：
xxx.json contains the annotated data information for both the training and validation sets.
thing_train2017:
xxx.npz is used to store the data information for the training dataset, stored in a NumPy array format.
train2017: training dataset.
val2017: test dataset.
